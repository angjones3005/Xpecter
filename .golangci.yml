version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck
    - ineffassign
  exclusions:
    paths:
      - build
      - frontend/node_modules
      - frontend/dist

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 5m
